Pushing a new line to this file fires .github/workflows/tts-probe.yml, which
answers whether the free pocket-tts pipeline can replace paid MiniMax TTS for
Persian narration, and whether HF_TOKEN is actually required to do it.

It runs in its own concurrency group, renders nothing, sends nothing, and
touches no delivery state. It is allowed to fail — a failure is the answer.

last touch: 2026-09-13T12:12:00Z
