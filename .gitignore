# secrets — never commit
.env

# Claude Code session-local runtime state (session id/pid/lock), not project content
.claude/

# generated / heavy
node_modules/
renders/
compositions/daily/
snapshots/
index.html
music/*.wav
*.output
c.txt
css.txt
.telegram-offset
music/auto/
music/voice/
.thumbnails/
*.log
.telegram-sent.json
lib/generated/
.media-cache/
renders/news/
compositions/news/
# german-lesson-build.mjs writes here every episode (renders/ above already
# covers the render itself); was missing from this list unlike daily/ and
# news/ above — owner report 2026-09-11: build-stage files should not
# linger after a video ships.
compositions/german/
.topic-proposals.json
public/broll/*.mp4
!public/broll/*-thread.mp4
public/screens/*.img
.topic-rejected.json
.topic-offered.json
# findLessonImage()/findRealImage() write real+test fixtures here every run
# (lesson-image.mjs, auto-image.mjs, pexels-image.mjs, wikimedia-image.mjs);
# never committed by the pipeline itself in 340+ real runs — same "generated
# / heavy" class as renders/ and compositions/ above. Test-run leftovers
# (test-lesson-image-ai-fallback.mjs etc. don't all clean up after
# themselves the way test-local-image-fallback.mjs's rmSync does) showed up
# as untracked files here 2026-09-15 — ignoring the whole directory instead
# of chasing each test.
public/user-media/
# german-cycle.mjs writes both on every run: the resolved provider command
# list, and the per-attempt diagnosis trail. They describe ONE run's state,
# like the render output above, and would otherwise churn a commit per build.
.provider-plan.json
.german-cycle-report.json
.provider-runtime-report.json
out/tune/
out/verify/
