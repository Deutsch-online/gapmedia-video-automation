# Provider List

این پرونده، فهرست عملیاتی سرویس‌های واقعی پروژه است. کلیدها فقط در `.env` یا GitHub Secrets نگهداری می‌شوند و هرگز در این فایل یا گزارش‌ها نوشته نمی‌شوند.

| قابلیت | اولویت | Provider | نوع | شرط آماده‌بودن |
| --- | ---: | --- | --- | --- |
| جست‌وجوی منبع | 1 | Exa | API | `EXA_API_KEY` |
| سیگنال تقاضا | 2 | Google Suggest | رایگان | بدون کلید |
| سیگنال گفتگو | 3 | Reddit | API | `REDDIT_CLIENT_ID` + `REDDIT_SECRET` |
| ترجمه و تحلیل | 1 | Gemini | API | `GEMINI_API_KEY` یا `GOOGLE_API_KEY` |
| ترجمه و تحلیل | 2 | Groq | API | `GROQ_API_KEY` |
| نریشن فارسی | 1 | MiniMax | API | `MINIMAX_API_KEY` + `MINIMAX_VOICE_ID` |
| نریشن فارسی | 2 | Microsoft Edge TTS | رایگان | بدون کلید |
| آزمون نریشن فارسی | — | Pocket TTS Farsi | رایگان / محلی | `POCKET_TTS_FARSI_CONFIG` |
| تحویل | 1 | Telegram Bot API | API | توکن و Chat ID تلگرام |

## رفتار اجرایی

1. Provider آمادهٔ اول امتحان می‌شود.
2. اگر پاسخ ندهد یا خطا دهد، Provider بعدی همان قابلیت خودکار امتحان می‌شود.
3. اجرای کار فقط وقتی متوقف می‌شود که همهٔ Providerهای قابل‌استفاده شکست بخورند.
4. فایل دریافت‌شده در `.provider-runtime-report.json` ثبت می‌شود؛ گزارش Telegram به مدیر می‌گوید چه Providerی فایل را آماده کرده و ورک‌فلو ادامه یافته است.
5. دستور تلگرام `پروایدرها` یا گزینهٔ `۲۸` وضعیت آماده‌بودن Providerها را نشان می‌دهد.

## چرخهٔ ساخت آلمانی (`german-cycle.mjs`)

دستور مالک ۱۴۰۵/۰۶/۲۴: ساخت ویدیوهای آلمانی در یک دور چرخشی انجام می‌شود، نه یک اجرای یک‌باره.

1. **لیست دستورات** — پیش از هر ساختی، همهٔ درخواست‌های build از همین رجیستری حل می‌شوند و در `.provider-plan.json` نوشته می‌شوند: هر قابلیت، ترتیب Providerها، و این‌که کدام Provider واقعاً کار را انجام می‌دهد.
   - `voice` و `delivery` الزامی‌اند؛ بدون آن‌ها قسمت قابل‌انتشار ساخته نمی‌شود و چرخه پیش از مصرف هیچ زمان رندری می‌ایستد.
   - `search` و `text` الزامی نیستند: LAW 7 در `lib/lesson-image.mjs` به گرافیک محلیِ برچسب‌دار ختم می‌شود، پس محدودیت نرخ این دو، انتشار را متوقف نمی‌کند.
2. **تلاش ساخت** — `german-lesson-build.mjs` بدون تغییر اجرا می‌شود و همهٔ گیت‌ها سر جای خودشان‌اند.
3. **دیاگنوز** — اگر تلاش شکست بخورد، خروجی واقعی آن به `lib/diagnose.mjs` می‌رود و نوع خطا با نام شناسایی می‌شود (نریشن، تصویر، تکراری، Provider، رندر، پیکربندی، اتمام محتوا).
4. **تصمیم** — چرخه فقط دو کار می‌تواند بکند: تلاش دوباره، یا ایستادن با دلیل. هیچ گیتی برای سبزشدن ضعیف نمی‌شود. اگر خطا عیناً تکرار شود (`no-progress`)، یا ذاتاً با تکرار درست نشود، یا بودجه تمام شود، چرخه می‌ایستد.
5. **تلگرام** — تا وقتی یک تلاش واقعاً موفق نشده، هیچ چیزی به کانال نمی‌رود. فقط ویدیوی آمادهٔ نهایی ارسال می‌شود؛ و اگر چرخه کاملاً شکست بخورد، یک گزارش تشخیصی (نه خطای خام) با سیر کامل تلاش‌ها فرستاده می‌شود.

Pocket TTS Farsi در لوکال و Cloud نصب/دریافت می‌شود، اما فعلاً فقط برای آزمون است: آزمون کیفیت تلفظ آن را برای انتشار خودکار تأیید نکرده است. بنابراین عمداً در زنجیرهٔ خودکارِ تولید قرار ندارد.
